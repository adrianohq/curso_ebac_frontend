const multiplicar = (numeroX: number, numeroY: number): number => {
    return numeroX * numeroY;
}

const saudacao =  (nome: string): string => {
    return "Olá " + nome;
}

console.log(multiplicar(5,3));
console.log(saudacao('Maria'));